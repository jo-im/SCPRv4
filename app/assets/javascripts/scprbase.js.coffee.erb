###
SCPRv4: Southern California Public Radio
###

window.scpr ?= {}

scpr.NEWSROOM   = "<%= Rails.configuration.x.newsroom.url %>"
API_PUBLIC_ROOT = "http://<%= Rails.configuration.x.scpr.host %>/api"

# stub console.log() for IE
if !window.console
    class window.console
        @log: ->
