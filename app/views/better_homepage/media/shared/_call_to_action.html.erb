<a class="link link--callout" href="<%= content.article.public_url %>">
  <% case content.call_to_action %>
  <% when 'conversation'%>
    <img src="img/icon_line-comment.svg" />
    Join the conversation
  <% when 'event' %>
    <%= image_tag 'better_homepage/event.svg' %>
    RSVP for this event
  <% when 'podcast' %>
    <%= image_tag 'better_homepage/podcast.svg' %>
    <% show_name = content.article.try(:show).try(:title) || "this show" %>
    Love <%= show_name %>? Subscribe to the podcast!
  <% end %>
</a>