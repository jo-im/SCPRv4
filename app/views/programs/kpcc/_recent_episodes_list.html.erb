<section class="episodes">
  <h2>Recent Episodes</h2>
  <nav>
    <ul>
      <% episodes.first(5).each do |episode| %>
        <li><a href="<%= episode.public_path %>">
          <time datetime="<%= format_date(episode.air_date, with:'%F') %>"><mark><%= format_date(episode.air_date, with: '%A') %></mark>, <%= format_date(episode.air_date, with: '%B %e, %Y') %></time>
          <span><%= episode.headline %></span></a></li>
      <% end %>
    </ul>
  </nav>
  <footer>
    <a href="/programs/<%= program.slug %>/featured?view=episodes"><span>See <cite><%= program.title %></cite>&rsquo;s episode archive</span></a>
  </footer>
</section><!--/ .episodes -->