<%= cache ['amp-news-story', @amp_record] do %>
  <% processor = Embeditor::Processor.new %>
  <% body = processor.process @amp_record.body %>
  <% processor.close %>
  <%= sanitize body, scrubber: AmpSanitizer.new %>
<% end %>