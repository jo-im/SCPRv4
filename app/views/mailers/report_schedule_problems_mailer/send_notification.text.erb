There seem to be a few problems with the current SCPR program schedule:

<% if @overlaps.any? %>
OVERLAPS
---------
<% @overlaps.each do |problem| %>
  <% overlapper = problem.shift %>
  * <%= overlapper.metadata.display_name %> from <%= overlapper.display_starts_at%> to <%= overlapper.display_ends_at%> overlaps the following: 
    <% problem.each do |o| %>
      - <%= o.metadata.display_name %> | <%= o.display_starts_at %> - <%= o.display_ends_at %>
    <% end %>
<% end %>
<% end %>

<% if @gaps.any? %>
GAPS
---------
<% @gaps.each do |gap| %>
  * <%= gap[0].metadata.display_name %> ends at <%= gap[0].display_ends_at %> but <%= gap[1].metadata.display_name %> starts at <%= gap[1].display_starts_at %>
<% end %>
<% end %>