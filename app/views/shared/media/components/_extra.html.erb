<% if (related = filter_related_content(content, contents)).any? %>
  <%= render partial: "shared/media/components/related", locals: {label: content.label, content: related} %>
<% else %>
  <%= render partial: "shared/media/components/call_to_action", locals: {content: content} %>
<% end %>
