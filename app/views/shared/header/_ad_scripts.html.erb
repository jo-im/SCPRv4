<!-- Google DFP Setup -->
<script type='text/javascript'>
  var googletag = googletag || {};
  googletag.cmd = googletag.cmd || [];
  (function() {
    var gads = document.createElement('script');
    gads.async = true;
    gads.type = 'text/javascript';
    var useSSL = 'https:' == document.location.protocol;
    gads.src = (useSSL ? 'https:' : 'http:') +
      '//www.googletagservices.com/tag/js/gpt.js';
    var node = document.getElementsByTagName('script')[0];
    node.parentNode.insertBefore(gads, node);
  })();
</script>

<script type='text/javascript'>
  googletag.cmd.push(function() {
<% DFP_ADS.each do |ad_key, ad| %>
    googletag.defineSlot('/127204706/<%=ad_key%>', <%=ad[:size]%>, '<%=ad[:id]%>').addService(googletag.pubads());
<% end %>
    googletag.pubads().enableSingleRequest();
    googletag.pubads().collapseEmptyDivs();
    googletag.enableServices();

  });
</script>


<!-- OAS SETUP -->
<script>
  <!--//configuration
  OAS_url = 'http://oascentral.publicradio.org/RealMedia/ads/';
  OAS_sitepage = 'scpr.org'; // Default. This may be overridden by a special ad target.

  (function() {
    // The regexp to match against the current pathname.
    // If you pass a sitepage property, it will be used to set OAS_sitepage.
    // If there is no sitepage property for the matched path, it will use
    // window.location.pathname.
    var specials = [
      { re: new RegExp("^/education/") },
      { re: new RegExp("^/blogs/education") },
      { re: new RegExp("^/health") },
      { re: new RegExp("^/obamacare") },
      { re: new RegExp("^/$"), sitepage: "/homepage" }
    ];

    // Run through the special ad targeting to see if there's a match.
    for (var i=0; i < specials.length; i++) {
      var special = specials[i];

      if (window.location.pathname.match(special.re)) {
        OAS_sitepage += special.sitepage || window.location.pathname;
        break;
      }
    }
  }());

  OAS_listpos = "<%= content_for?(:oas_listpos) ? yield(:oas_listpos) : 'x01,x02,x03,x13' %>";
  OAS_query = '';
  OAS_target = '_top';
  //end of configuration
  OAS_version = 10;
  OAS_rn = '001234567890'; OAS_rns = '1234567890';
  OAS_rn = new String (Math.random()); OAS_rns = OAS_rn.substring (2, 11);
  function OAS_NORMAL(pos) {
    document.write('<a href="' + OAS_url + 'click_nx.ads/' + OAS_sitepage + '/1' + OAS_rns + '@' + OAS_listpos + '!' + pos + '?' + OAS_query + '" target=' + OAS_target + '>');
    document.write('<img src="' + OAS_url + 'adstream_nx.ads/' + OAS_sitepage + '/1' + OAS_rns + '@' + OAS_listpos + '!' + pos + '?' + OAS_query + '" border="0" /></a>');
  }
  //-->
</script>

<script>
  <!--
  OAS_version = 11;
  if ((navigator.userAgent.indexOf('Mozilla/3') != -1) || (navigator.userAgent.indexOf('Mozilla/4.0 WebTV') != -1)) {
    OAS_version = 10;
  }
  if (OAS_version >= 11) {
    document.write('<SCR' + 'IPT LANGUAGE=JavaScript1.1 SRC="' + OAS_url + 'adstream_mjx.ads/' + OAS_sitepage + '/1' + OAS_rns + '@' + OAS_listpos + '?' + OAS_query + '"><\/SCRIPT>');
  }
  //-->
</script>

<script>
  <!--
  document.write('');
  function OAS_AD(pos) {
    if (OAS_version >= 11) {
      OAS_RICH(pos);
    } else {
      OAS_NORMAL(pos);
    }
  }
  //-->
</script>

<!-- chartbeat timing for load start -->
<script type="text/javascript">var _sf_startpt=(new Date()).getTime()</script>
