<header class="preface">
    <h1><%= issue.title %></h1>
    <div class="details clearfix">
        <div class="brief">
            <p><%= issue.description %></p>
        </div>
        <aside class="timeline">
            <h2>KPCC has published a total of <mark><%= articles.results %> articles</mark> on <%= issue.title %></h2>
        <% if issue.began_at && issue.most_recent_at %>
          <div class="range">
              <dl class="began">
                  <dt><%= format_date(issue.began_at, with: '%-d %^b %Y') %></dt>
                  <dd>Coverage began</dd>
              </dl>
              <dl class="latest">
                  <dt><%= format_date(issue.most_recent_at, with: '%-d %^b %Y') %></dt>
                  <dd>Latest coverage</dd>
              </dl>
          </div>
        <% end %>

        </aside>
    </div> <!--/ .details -->
</header>